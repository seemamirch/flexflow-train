ff_add_library(
  NAME
    compiler
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    op-attrs
    utils
    deps::json
    pcg
    deps::spdlog
    substitutions
)

add_subdirectory(ffi)
add_subdirectory(test)
add_subdirectory(benchmark)
